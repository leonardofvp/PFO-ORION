extends layout

block titulo_seccion
  .row.justify-content-center
    .col-12.col-lg-8
      h2.mt-4.mb-4= editable ? "Editar Subcontratista: " + subcontratista.nombre : "Registrar Nuevo Subcontratista"

block content
  .row.justify-content-center
    .col-12.col-lg-8
      .card.border-primary.shadow-sm.mb-5
        .card-body.p-4

          form(method="POST", action= editable ? `/subcontratistas/editar-subcontratista/${subcontratista.id}?_method=PUT` : `/subcontratistas/nuevo-subcontratista`)

            .row
              .col-md-6.mb-3
                label.form-label.fw-bold(for="nombre") Nombre o Razón Social
                input#nombre.form-control(type="text", name="nombre", value= subcontratista ? subcontratista.nombre : "", required)

              .col-md-6.mb-3
                label.form-label.fw-bold(for="cuit") CUIT
                input#cuit.form-control(type="text", name="cuit", value= subcontratista ? subcontratista.cuit : "", readonly=editable, required)

            .row
              .col-md-6.mb-3
                label.form-label.fw-bold(for="especialidad") Especialidad
                input#especialidad.form-control(type="text", name="especialidad", value= subcontratista ? subcontratista.especialidad : "", required)

              .col-md-6.mb-3
                label.form-label.fw-bold(for="telefono") Teléfono
                input#telefono.form-control(type="text", name="telefono", value= subcontratista ? subcontratista.telefono : "", required)

            .mb-3
              label.form-label.fw-bold(for="email") Email
              input#email.form-control(type="email", name="email", value= subcontratista ? subcontratista.email : "", required)

            hr.my-4

            .d-flex.justify-content-end.gap-2
              if editable
                a.btn.btn-outline-secondary(href=`/subcontratistas/detalle-subcontratista/${subcontratista.id}`) Cancelar
              else
                a.btn.btn-outline-secondary(href="/subcontratistas") Volver al listado

              button.btn.btn-primary(type="submit")
                i.bi(class= editable ? "bi-pencil-square me-2" : "bi-check-lg me-2")
                | #{editable ? "Guardar cambios" : "Crear subcontratista"}