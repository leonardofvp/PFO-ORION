extends layout

block titulo_seccion
  .d-flex.justify-content-between.align-items-center.mt-4.mb-4
    h2 Gestión de Usuarios

block content
  .card.border-primary.shadow-sm.mb-5.overflow-hidden

    .card-body.p-0
      .table-responsive
        table.table.table-hover.align-middle.mb-0
          thead.table-light
            tr
              th.px-4 Nombre
              th Apellido
              th Rol
              th.text-center Acciones

          tbody
            each usuario in usuariosActivos
              tr
                td.px-4.fw-bold= usuario.nombre
                td= usuario.apellido
                td
                  span.badge.bg-secondary= ROLES[usuario.rol.toUpperCase()] ? ROLES[usuario.rol.toUpperCase()].label : usuario.rol
                td.text-center
                  a.btn.btn-sm.btn-outline-primary(href=`/usuarios/detalle-usuario/${usuario.id}`)
                    i.bi.bi-eye.me-1
                    | Ver detalle

            else
              tr
                td(colspan="4").text-center.py-4.text-muted
                  i.bi.bi-info-circle.me-2
                  | No hay usuarios registrados en el sistema.

  .d-flex.justify-content-end.mt-4.mb-5
    a.btn.btn-outline-secondary.mb-5(href="/")
      i.bi.bi-arrow-left.me-2
      | Volver al inicio